(function(){'use strict';const STORAGE_KEY='quant_notifications';const READ_KEY='quant_notifications_read';const MAX_NOTIFICATIONS=50;function make(type,priority,title,message,code=null,extra={}){return{id:`${type}-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,type,priority,title,message,code,timestamp:new Date().toISOString(),...extra};}
function generateFromData({portfolio,top,distribution,movers,supplySignals,log}){const notifs=[];if(log&&log.length>0){const last=log[0];const timeAgo=minutesAgo(last.run_at);if(last.status==='ok'){notifs.push(make('data_update','normal','✅ 데이터 갱신 완료',`${timeAgo}분 전 · ${last.message || '정상'} (${(last.duration_ms / 1000).toFixed(1)}초)`,null,{duration_ms:last.duration_ms}));}else if(last.status==='error'){notifs.push(make('data_update','high','⚠️ 마지막 갱신 실패',last.message||'오류 발생 — 자동 재시도 대기 중',null,{isError:true}));}}
if(portfolio&&portfolio.items){portfolio.items.slice(0,10).forEach((it)=>{const totalScore=it.total_score||0;if(totalScore>=80){notifs.push(make('high_score','high',`🌟 A+ 종목: ${it.name}`,`${it.code} · ${totalScore.toFixed(1)}점 · 섹터: ${it.sector || '—'}`,it.code,{grade:it.grade?.letter}));}
else if(totalScore>=70){notifs.push(make('signal_change','normal',`🟢 매수 후보: ${it.name}`,`${it.code} · ${totalScore.toFixed(1)}점 (Buy)`,it.code,{grade:it.grade?.letter}));}});}
if(movers){if(movers.gainers){movers.gainers.slice(0,3).forEach((m)=>{notifs.push(make('signal_change','high',`🚀 급등: ${m.name} +${(m.change_pct || 0).toFixed(2)}%`,`${m.code} · ${m.market || ''} · 현재가 ${(m.close || 0).toLocaleString()}원`,m.code));});}
if(movers.losers){movers.losers.slice(0,3).forEach((m)=>{notifs.push(make('signal_change','high',`📉 급락: ${m.name} ${(m.change_pct || 0).toFixed(2)}%`,`${m.code} · ${m.market || ''} · 현재가 ${(m.close || 0).toLocaleString()}원`,m.code));});}}
if(supplySignals){if(supplySignals.buy){supplySignals.buy.slice(0,2).forEach((s)=>{notifs.push(make('signal_change','normal',`💰 외인+기관 매수: ${s.name}`,`${s.code} · 5일 누적 ${(s.foreign_5d || 0).toLocaleString()}주`,s.code));});}}
if(distribution&&distribution.summary){const s=distribution.summary;notifs.push(make('system','low','📊 시장 분포 요약',`${s.count}개 종목 · 평균 ${s.mean.toFixed(1)}점 · 표준편차 ${s.std.toFixed(1)}`,null,{summary:s}));}
return notifs;}
function minutesAgo(iso){if(!iso)return 0;const t=new Date(iso).getTime();if(!Number.isFinite(t))return 0;return Math.max(0,Math.round((Date.now()-t)/60000));}
function load(readOnly=false){try{const list=JSON.parse(localStorage.getItem(STORAGE_KEY)||'[]');const read=new Set(JSON.parse(localStorage.getItem(READ_KEY)||'[]'));return list.map((n)=>({...n,read:read.has(n.id)}));}catch(e){return[];}}
function save(list){try{const trimmed=list.slice(0,MAX_NOTIFICATIONS);const ids=trimmed.map((n)=>n.id);localStorage.setItem(STORAGE_KEY,JSON.stringify(trimmed));localStorage.setItem(READ_KEY,JSON.stringify(ids.filter((id)=>{const item=trimmed.find((n)=>n.id===id);return item&&item.read;})));}catch(e){}}
function mergeAndSave(newOnes){const existing=load();const map=new Map(existing.map((n)=>[n.id,n]));newOnes.forEach((n)=>map.set(n.id,{...n,read:map.get(n.id)?.read||false}));const merged=Array.from(map.values()).sort((a,b)=>new Date(b.timestamp)-new Date(a.timestamp)).slice(0,MAX_NOTIFICATIONS);save(merged);return merged;}
function markAllRead(){try{const list=JSON.parse(localStorage.getItem(STORAGE_KEY)||'[]');const read=list.map((n)=>n.id);localStorage.setItem(READ_KEY,JSON.stringify(read));}catch(e){}}
function clear(){try{localStorage.removeItem(STORAGE_KEY);localStorage.removeItem(READ_KEY);}catch(e){}}
window.NotifStore={generateFromData,load,save,mergeAndSave,markAllRead,clear,MAX_NOTIFICATIONS};console.log('[notifications] loaded');})();